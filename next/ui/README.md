# WePush Next UI

Web 与 Desktop 共用 React Features、API Client、Schema Renderer、UI primitives 和 Design Tokens。

```bash
cd next/ui
corepack pnpm install
corepack pnpm check
corepack pnpm dev
```

开发服务器把 `/api`、`/actuator` 和 `/openapi.yaml` 代理到 `http://127.0.0.1:18990`。
