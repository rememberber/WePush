{
  "name": "@wepush-next/desktop",
  "version": "0.1.0-alpha.3",
  "private": true,
  "type": "module",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "pnpm build && electron .",
    "package": "pnpm --filter @wepush-next/web build && pnpm build && node scripts/package.mjs"
  },
  "dependencies": {
    "@wepush-next/web": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "electron": "43.4.1",
    "typescript": "catalog:"
  }
}
